<h1>Add Bookmark to <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark], html: { class: 'custom-form' } do |f| %>
  <%= f.input :comment  %>
  <%= f.association :movie %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>

<%= link_to 'Back to List', list_path(@list) %>
